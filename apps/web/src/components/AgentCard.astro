---
import Glyph from "./Glyph.astro";
import type { AgentCard } from "../lib/data";

interface Props {
  agent: AgentCard;
}
const { agent } = Astro.props;
const c = agent.color;
---

<a
  href={`/agents/${agent.name}/`}
  class="agent-card agent-item group flex h-full flex-col gap-[10px] rounded-lg border border-edge bg-panel p-[15px]"
  style={`--famh:${c}66`}
  data-family={agent.family}
>
  <div class="flex items-center gap-[9px]">
    <div
      class="flex h-8 w-8 flex-none items-center justify-center rounded-[7px]"
      style={`background:${c}14;border:1px solid ${c}33`}
    >
      <Glyph glyph={agent.glyph} color={c} scale={4} />
    </div>
    <div class="font-pixel text-[9px] leading-[1.4] tracking-[0.01em]" style={`color:${c}`}>
      {agent.categoryLabel.toUpperCase()}
    </div>
  </div>

  <div class="text-[15.5px] font-semibold leading-[1.3] text-ink transition-colors group-hover:text-eve">
    {agent.title}
  </div>
  <div class="line-clamp-2 text-[13.5px] leading-[1.5] text-muted">{agent.description}</div>
</a>
