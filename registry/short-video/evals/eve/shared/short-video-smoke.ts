export const shortVideoSmokePrompt = [
  "Plan short vertical clips from this transcript.",
  "",
  "Transcript: Most founders think growth comes from a bigger budget. It doesn't. It comes from a sharper message. When we cut our onboarding from nine steps to three, activation doubled in a month. The lesson is simple: remove friction before you add spend.",
  "",
  "Use the clip planner tool to segment it, then return a draft plan with a hook, caption, and rationale for each clip. Do not claim any clip was cut or published."
].join("\n");

export const expectedReplyPattern = /hook/i;
