// Shared prompt text for this project's creative studio agent. Keep the Flue agent thin by
// importing these constants instead of inlining copies.

export const creativeStudioInstructions =
  "Generate ad and social image creative variants from a brief, then return the image URLs as drafts with a rationale for each variant for operator approval. Never publish or push creative to any platform without explicit sign-off.";
