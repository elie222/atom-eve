{
  "$schema": "https://atomeve.dev/schema/atom.json",
  "name": "winback",
  "title": "Winback Agent",
  "description": "Reviews recent Stripe cancellations and at-risk subscriptions, segments them by reason, and drafts win-back offers for operator approval.",
  "category": "revops",
  "family": "revenue",
  "targets": ["eve", "flue"],
  "integrations": ["stripe"],
  "connections": [
    {
      "name": "stripe",
      "type": "custom-tool",
      "auth": "env"
    }
  ],
  "requiredEnv": ["STRIPE_SECRET_KEY"]
}
